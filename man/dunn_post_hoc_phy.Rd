% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dunn_post_hoc_phy.R
\name{dunn_post_hoc_phy}
\alias{dunn_post_hoc_phy}
\title{Perform Dunn's post-hoc tests on multiple alpha diversity indices from a phyloseq object}
\usage{
dunn_post_hoc_phy(physeq, measures = NULL)
}
\arguments{
\item{physeq}{A \code{phyloseq} object.}

\item{measures}{Character vector of alpha diversity measures to use.
Default is all available: \code{c("Observed", "Chao1", "ACE", "Shannon", "Simpson", "InvSimpson", "Fisher")}.}
}
\value{
A \code{data.frame} with Dunn's test results: test index, grouping variable, comparison, z-scores, and p-values.
}
\description{
This function calculates alpha diversity indices for a phyloseq object
and performs Dunn's tests across all grouping variables with ≥3 levels.
}
\examples{
library(phyloseq)
data(GlobalPatterns)
dunn_results <- dunn_post_hoc_phy(GlobalPatterns)
head(dunn_results)
}
